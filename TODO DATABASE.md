<!--
    When running the migration.php script, there's only one table created : user
    (the first table in the list in "database.sql")
    Why is that ? Where are the tables "article", "image" and "article_has_image" ?!!
 -->
