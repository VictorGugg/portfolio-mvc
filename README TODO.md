<!--
    Gather the separate "functionnal" views (add, edit, delete) inside the home page.

    Allow to actually upload a file through the form (store the file inside the server, store the url inside the db)
    Display the actual images

    Create a "gallery" section/page to display all images and articles created by the author.
    Allow CRUD functions inside the gallery and specific art pages too.

 -->
